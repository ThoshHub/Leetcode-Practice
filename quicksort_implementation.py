import random

def qsort(s):
    # if array is 0 or 1 long, then return it
    if(len(s) < 2): # if the 
        return s
    elif(len(s) == 2):
        # check

    else:
        # otherwise will need to do recursion
        # pick a pivot (random)
        # take everything before the array and put it into new array "a"
        # take everything before the array

    return []

a = []
b = [1]
c = [2, 1]
d = [3, 5, 2, 43, 12, 4, 5, 7, 88, 432, 23]

print(qsort(a))